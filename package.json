{
  "name": "codex-personal-work-patcher",
  "private": true,
  "version": "0.3.0",
  "description": "Native-feeling personal/work account routing for Codex local and SSH tasks",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nvemuri4649/codex-subscription-router.git"
  },
  "homepage": "https://github.com/nvemuri4649/codex-subscription-router#readme",
  "bugs": {
    "url": "https://github.com/nvemuri4649/codex-subscription-router/issues"
  },
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "npm run check:go && npm run check:js && npm run check:python && npm run check:shell && npm run check:native",
    "check:go": "go test ./... && go vet ./...",
    "check:js": "node --check ui/account-menu.js && node --check ui/thread-subscription.js && node --check ui/ui-test-bridge.cjs && npm run test:js",
    "test:js": "node --test ui/*.test.cjs",
    "check:python": "python3 -m py_compile scripts/*.py && npm run test:python",
    "test:python": "python3 -m unittest discover -s scripts -p \"test_*.py\"",
    "check:shell": "bash -n install.sh",
    "check:native": "xcrun clang -fsyntax-only -Wall -Wextra native/launcher.c",
    "release:check": "python3 scripts/check_release.py",
    "check:personal-work": "npm run check"
  },
  "devDependencies": {
    "@electron/asar": "4.2.1"
  }
}
